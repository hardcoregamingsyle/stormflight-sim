name: release-eos
# Builds a Windows .exe with the Epic Online Services plugin bundled, so
# MULTIPLAYER -> QUICK JOIN can reach a public server without the player
# installing anything. Isolated from the normal release: it publishes a
# separate prerelease tag, so if the plugin download/export ever breaks it
# never touches the stable v1.3.0 build.
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'core/net/**'
      - '.github/workflows/release-eos.yml'
permissions:
  contents: write
concurrency:
  group: release-eos
  cancel-in-progress: true
env:
  GODOT_VERSION: 4.7-stable
  EOS_PLUGIN_TAG: "2.3.0"
  RELEASE_TAG: eos-preview
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Godot and export templates
        run: |
          curl -sSL -o godot.zip "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}/Godot_v${GODOT_VERSION}_linux.x86_64.zip"
          unzip -q godot.zip
          sudo mv "Godot_v${GODOT_VERSION}_linux.x86_64" /usr/local/bin/godot
          curl -sSL -o templates.tpz "https://github.com/godotengine/godot/releases/download/${GODOT_VERSION}/Godot_v${GODOT_VERSION}_export_templates.tpz"
          TPLDIR="$HOME/.local/share/godot/export_templates/${GODOT_VERSION/-/.}"
          mkdir -p "$TPLDIR"
          unzip -q templates.tpz
          mv templates/* "$TPLDIR"/

      - name: Fetch and install the EOS plugin
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -e
          mkdir -p eos_dl _eos_extract
          # Grab the plugin release assets (zips) from the public plugin repo.
          gh release download "$EOS_PLUGIN_TAG" --repo 3ddelano/epic-online-services-godot --pattern '*.zip' --dir eos_dl \
            || gh release download "$EOS_PLUGIN_TAG" --repo 3ddelano/epic-online-services-godot --dir eos_dl
          echo "Downloaded:"; ls -la eos_dl
          for z in eos_dl/*.zip; do [ -f "$z" ] && unzip -o -q "$z" -d _eos_extract || true; done
          # Find the addon folder wherever it landed and copy it into the project.
          SRC=$(find _eos_extract -type d -name 'epic-online-services-godot' | head -1)
          echo "Addon folder: '$SRC'"
          if [ -z "$SRC" ]; then echo "::error::EOS addon folder not found in release assets"; find _eos_extract -maxdepth 3 -type d; exit 1; fi
          mkdir -p addons
          cp -r "$SRC" addons/
          echo "Installed addon contents:"; ls -la addons/epic-online-services-godot

      - name: Import project (registers the GDExtension)
        run: |
          godot --headless --import 2>&1 | tail -30 || true

      - name: Export Windows (EOS-enabled)
        run: |
          mkdir -p builds/windows
          godot --headless --export-release "Windows Desktop" builds/windows/StormfighterFlightSim-EOS.exe 2>&1 | tail -40
          echo "Exported files (the plugin emits native DLLs beside the exe):"
          ls -la builds/windows
          test -s builds/windows/StormfighterFlightSim-EOS.exe
          # A GDExtension can't live inside a single .exe - its native DLLs
          # (EOS SDK + plugin) must sit next to it, so ship the whole folder
          # as one zip. Confirm the two critical libs are present.
          test -f builds/windows/EOSSDK-Win64-Shipping.dll || (echo "::error::EOS SDK dll missing from export" && exit 1)
          ls builds/windows/libeosg*.dll >/dev/null 2>&1 || (echo "::error::plugin dll missing from export" && exit 1)
          cd builds/windows
          zip -q -j StormfighterFlightSim-EOS.zip *.exe *.dll
          unzip -l StormfighterFlightSim-EOS.zip

      - name: Publish EOS preview release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cat > notes.md <<'EOF'
          **EOS multiplayer preview** (Windows) — the same game as the stable release, with Epic Online Services bundled so **MULTIPLAYER → QUICK JOIN** reaches a shared public server (no plugin install, no account, no IP).

          ### Download `StormfighterFlightSim-EOS.zip`, unzip it, and run the .exe **from the unzipped folder** — keep the `.dll` files next to it. (A native plugin can't be a single self-contained .exe; the DLLs must stay beside the game.)

          Auto-assembled by CI and **not run-tested** (CI can't launch a real multiplayer session). Hit Quick Join and watch the status line — if it doesn't connect it names the exact step to fix. The stable v1.3.0 single-exe download is unaffected.
          EOF
          gh release delete "$RELEASE_TAG" --yes --cleanup-tag || true
          gh release create "$RELEASE_TAG" builds/windows/StormfighterFlightSim-EOS.zip \
            --title "EOS multiplayer preview" --notes-file notes.md --prerelease
